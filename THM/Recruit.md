Смотрим открытые порты.
```
Host is up (0.015s latency).
Not shown: 65532 closed tcp ports (reset)
PORT   STATE SERVICE
22/tcp open  ssh
53/tcp open  domain
80/tcp open  http

Nmap done: 1 IP address (1 host up)
```

Далее изучим подробнее сервисы на открытых портах.
```
Host is up (0.014s latency).

PORT   STATE SERVICE VERSION
22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.7 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey:
|   3072 3d:b7:98:c1:29:0e:55:77:68:0c:dc:62:77:26:0a:57 (RSA)
|   256 4f:9f:ed:bf:f0:00:db:b2:21:41:f2:b4:47:9f:e2:18 (ECDSA)
|_  256 56:a1:06:57:77:6e:30:bf:f4:34:2a:1a:82:49:3b:14 (ED25519)
53/tcp open  domain  ISC BIND 9.16.1 (Ubuntu Linux)
| dns-nsid:
|_  bind.version: 9.16.1-Ubuntu
80/tcp open  http    Apache httpd 2.4.41 ((Ubuntu))
|_http-title: Recruit
| http-cookie-flags:
|   /:
|     PHPSESSID:
|_      httponly flag not set
|_http-server-header: Apache/2.4.41 (Ubuntu)
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel

Service detection performed.
```

Переходим к изучению веб-сервера. Фаззингом находим некоторые страницы.

![](../images/Pasted%20image%2020260618001326.png)

На странице api.php указывается, что это за апи, для чего оно, как им управлять. Нам необходимо на странице file.php передать параметр cv - внутренний файл, на который не установлены ограничения.

![](../images/Pasted%20image%2020260618001423.png)

Просто попробовал передать цифру, но ничего не вернулось, потому что, скорее всего, надо использовать file://

![](../images/Pasted%20image%2020260618001749.png)

Далее посмотрим что такое директория mail. В ней можно найти mail.log.
В этом письме говориться об развертывании сайта с резюме, далее, что логин у рекрутера hr, а пароль лежит в config.php (если переходить на эту страницу, то она пустая). Пароль администратора лежит в БД.

![](../images/Pasted%20image%2020260618002030.png)

В sitemap.xml ничего нового, также как в assets. В assets только core.js интересно выглядит.
Я изучил этот скрипт и понял, что core.js - это стандартная минифицированная библиотека Typewriter.js для эффекта печатания текста. В общем, ничего интересного.

Перейдем на страницу file.php и передадим в cv тот самый config.php, где должны быть учетные данные для профиля hr.

И действительно, файл открывается с помощью внутреннего запроса уязвимостью SSRF

![](../images/Pasted%20image%2020260618112040.png)

Полученные креды
```
hr:hrpassword123
```

Заходим на сайт и читаем флаг пользователя. Далее перед собой мы видим таблицу и поле поиска. Проверяем на SQL инъекцию.

![](../images/Pasted%20image%2020260618112245.png)

Обычная кавчка уронила синтаксис SQL и теперь точно SQL инъекция подтверждена.

![](../images/Pasted%20image%2020260618112336.png)

Подбираем количество столбцов, хотя скорее всего их будет 4, как в исходной таблице.

![](../images/Pasted%20image%2020260618112649.png)

На цифре 4 ошибка исчезла. Можно перейти к вытаскиванию данных из БД.

![](../images/Pasted%20image%2020260618112845.png)

Отобразим текущую БД и версию СУБД. Мы находимся в БД recruit_db, а у СУБД версия 8.0.33

![](../images/Pasted%20image%2020260618113018.png)

Посмотрим какие есть ещё БД. Из них всех интереснее всего выглядит recruit_db, кроме того я узнал что в этой БД есть таблицы users.

![](../images/Pasted%20image%2020260618113207.png)

Посмотрим какие есть колонки в таблице users БД recruit_db. Там есть имя пользователя и пароль. Достанем их.

![](../images/Pasted%20image%2020260618114709.png)

Получаем логин и пароль админа.

![](../images/Pasted%20image%2020260618114822.png)

Заходим на сайт и читаем флаг администратора веб-приложения.

![](../images/Pasted%20image%2020260618114905.png)
